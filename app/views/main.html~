<div id="container">


<hr style="color: red; height: 5px;background-color: #f00;width:100%;position:absolute;  left:0; top:110px; z-index: -2;   border-top: 0px;" > </hr>
    <div id="{{ticket.id}}" ng-repeat="ticket in search.results" style ="position:absolute;  top:{{Math.max((analyze(ticket).timeLeft/2500000))+110 }}px; left:{{getColumn(25)}}px; " >
      
      <div class="{{analyze(ticket).supportLevel}} arrow" style="position: absolute;  top: 50%;  transform: translateY(-50%); left:-7%" ></div>
      <div title="{{analyze(ticket).timeLeft/1000/60}}" class="{{analyze(ticket).supportLevel}} {{analyze(ticket).ticketSeverity}} none ticket" style="position:right; border-style: ridge;">

         <span style=""> <p> <i class="tiny mdi-action-label" style=""><a href="http://getchef.zendesk.com/agent/tickets/{{ticket.id}}" target="_blank">{{ticket.id}}</i></a></p></span>         <span class="white-text">


        </span>
      </div>

    </div>

</div>
